{
	"name": "ar-mcp",
	"version": "0.1.0",
	"description": "AnchorRegistry MCP server — verify-only v0.1",
	"private": true,
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/AnchorRegistry/ar-mcp.git"
	},
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"format": "oxfmt --write .",
		"lint:fix": "oxlint --fix",
		"start": "wrangler dev",
		"cf-typegen": "wrangler types",
		"type-check": "tsc --noEmit",
		"test": "vitest run"
	},
	"dependencies": {
		"agents": "^0.9.0",
		"zod": "^4.3.6"
	},
	"devDependencies": {
		"oxfmt": "^0.43.0",
		"oxlint": "^1.58.0",
		"typescript": "6.0.2",
		"vitest": "^2.1.9",
		"wrangler": "^4.79.0"
	}
}
